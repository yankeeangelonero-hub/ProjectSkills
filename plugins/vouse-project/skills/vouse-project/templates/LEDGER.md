# LEDGER — <FILL: project name>

Append-only, newest first. One dated entry per event: a unit opened or
closed, a canon change, a delivery. Written as prose for a cold human
reader, in the bob specification register (see the `bob-write` skill) —
what happened, what it means, what was deferred and why, the owner's
approval in their own words. Cite `record/` files for numbers; never
re-derive them here.

## <FILL: YYYY-MM-DD — project seeded>

<FILL: 2–4 short paragraphs: the owner's brief (verbatim where possible),
what was seeded, what the first unit is, what is deliberately deferred.>
