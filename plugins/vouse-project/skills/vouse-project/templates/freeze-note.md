# Freeze — {{ID}} ({{DATE}})

Written BEFORE any outcome of this campaign is read. If the campaign is
one-shot, a corrected attempt after unblinding is a NEW campaign with a new
freeze; this one's finding stands either way.

## Input pins

<!-- generate: python scripts/pin_inputs.py --files <config files...> -->
<FILL: the pin table from pin_inputs.py — a freeze without pins is not a freeze.>

## Versions and seeds

| What | Value |
|---|---|
| Engine / tool versions | <FILL> |
| Seeds | <FILL> |

## Derivation lineage (first-order — do not skip)

For every input derived from source data, state HOW: assignment rules,
filters, class mappings, remaps — with the generating script/invocation.
An unpinned lineage can move results while every hash above stays green.

| Input | Derived from | Rule / invocation |
|---|---|---|
| <FILL> | <FILL> | <FILL> |

## Pre-registered comparison

<FILL: bands, scored cell, reference data — copied from or cited by the campaign file.>
